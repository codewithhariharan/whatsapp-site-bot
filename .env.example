WHATSAPP_TOKEN=your_whatsapp_permanent_token
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
WEBHOOK_VERIFY_TOKEN=make_up_any_random_string
APP_SECRET=your_meta_app_secret

# ── Cloud SQL for PostgreSQL ──────────────────────────────────────────────────
# No host/port: the Cloud SQL Python Connector addresses the instance by name.
INSTANCE_CONNECTION_NAME=your-project:asia-southeast1:site-bot-db
DB_NAME=sitebot
# With IAM auth, DB_USER is the service-account email WITHOUT the
# ".gserviceaccount.com" suffix, and DB_PASSWORD stays empty.
DB_USER=site-bot-sa@your-project.iam
DB_PASSWORD=
DB_IAM_AUTH=true
DB_PRIVATE_IP=false

# ── Claude ────────────────────────────────────────────────────────────────────
# Two backends. Set VERTEX_PROJECT_ID for Vertex, or leave it empty and set
# ANTHROPIC_API_KEY for the direct API. Vertex wins if both are set.
#
# Vertex is preferred — auth is Application Default Credentials (the VM service
# account), so no key sits on disk — but it needs the GCP project to hold a
# Vertex AI Model Garden grant for Anthropic models. Without that grant every
# call 404s, and on an org-managed project the grant is someone else's approval.
VERTEX_PROJECT_ID=your-project
VERTEX_REGION=global
ANTHROPIC_API_KEY=

# Configure the Vertex spelling of the model ids. Dated snapshots use an "@" on
# Vertex and carry no date at all on the direct API (claude-haiku-4-5), so
# ai_client.model_id() strips the suffix when the direct API is in use.
PARSER_MODEL=claude-haiku-4-5@20251001
ANSWER_MODEL=claude-sonnet-4-6

# ── Baileys bridge (group messaging via unofficial WhatsApp Web) ──────────────
BAILEYS_BRIDGE_URL=http://bridge:8088
BRIDGE_SHARED_SECRET=must_match_the_bridge_BRIDGE_SHARED_SECRET
# Only accept logs from these group JIDs (comma-separated).
ALLOWED_GROUP_IDS=1203630xxxxxxxxxx@g.us
